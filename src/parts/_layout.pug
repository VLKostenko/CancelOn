doctype html
//- Global variables
- var page
block vars
//- Template
html(lang="en")
   head
      include ../parts/head
   body#body(class=page.classes)
      block main
      - if ( page.name === 'update-browser' || page.name === 'explore' ) {

      - } else {
         include ../parts/footer
      - }
      //if page.name !== 'update-browser' || page.name !== 'explore'
      include ../parts/scripts
